/*
 * This file is auto-generated using generate-artifacts script. Any changes will be reverted
 */
import * as Web3 from "web3";

/* tslint:disable ordered-imports */
{{#each files}}
import * as {{capitalize this.name}}Artifact from "../../artifacts/{{capitalize this.name}}.json";
{{/each}}

{{#each files}}
import { {{capitalize this.name}}EventArgs } from "./{{snakecase this.name}}";
{{/each}}
/* tslint:enable ordered-imports */

export type CivilEventArgs =
  {{#each files}}
    {{capitalize this.name}}EventArgs{{#unless @last}} |{{/unless}}{{^unless @last}};{{/unless}}
  {{/each}}

export interface Artifact {
  contractName: string;
  abi: Web3.ContractAbi;
  bytecode: string;
  deployedBytecode: string;
  networks: {
    [id: string]: {
      address: string;
    };
  };
}

export const artifacts = {
  {{#each files}}
  {{capitalize this.name}}: {{capitalize this.name}}Artifact as any as Artifact,
  {{/each}}
};
